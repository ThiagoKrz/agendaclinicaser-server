spring:
  config:
    activate:
      on-profile: prod

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:6543/postgres?user=postgres.mgptkrvuirdpqlbcotdi&password=gQvklPlpi02afeHo


server:
  port: 8080
  max-http-request-header-size: 1MB

logging:
  charset:
    file: logs/uniforum/application_prod.log
  pattern:
    console: "%d %-5level %logger : %msg%n"
    file: "%d %-5level [%thread] %logger : %msg%n"
  level:
    web: ERROR
    root: INFO
    sql: ERROR
